# Local PostgreSQL instance started by compose.yaml.
# Port 5433 on the host so it does not clash with a local Postgres on 5432.
DATABASE_URL="postgresql://bookmark_manager:bookmark_manager@localhost:5433/bookmark_manager?schema=public"
